{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Opener (system browser for OAuth) and deep-link (OAuth callback) — nothing else. Explicitly extended to the remote kalimat.site origin since the window loads a live URL, not bundled local content (Tauri blocks IPC for remote content by default).",
  "windows": ["main"],
  "remote": {
    "urls": ["https://kalimat.site/*"]
  },
  "permissions": [
    "opener:default",
    "deep-link:default"
  ]
}
